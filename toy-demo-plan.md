# 玩具模型 demo 计划:从零训练的纯 GDN 小模型及其电路运行

**版本** v1.0 · 2026-09-21
**状态** 设计稿,待评审。评审通过后据此写实施计划。
**取代** project-plan.md 的 Stage 2–4 与 two-stage-project-plan.md 的模型侧路线。Stage 0 规范(folded-T1-spec.md 0.1.3)与 Stage 1 的电路原语工作不变,本计划直接使用它们。

## 1. 目的与范围变化

原计划以 Qwen3.5-0.8B 为基座,对一层原生 GDN 层做脉冲化改造并微调,再把 6 层 softmax 注意力转成 GDN、蒸馏出完整语言模型。本计划改为:**从零训练一个所有层都是 GDN 的小模型,只要求它复现训练语料里的几句话,并在行为级电路仿真器上跑通。** 目标是一个演示机制的玩具,不是可用的语言模型。

这一改变去掉的工作:Qwen 权重下载与 LoRA 微调、6 层 softmax 的转换、24 层蒸馏、WikiText-2 困惑度门。保留的工作:规范 0.1.3 的全部约定(形式 A、编码表、固定缩放、累加器残差、相位表)、Stage 1 的行为级原语、以及原 Stage 2.1 的"浮点层 → 可脉冲化层 → 折叠转换 → 短微调"路径。这条路径在本计划里作用于从零训练的模型,而不是 Qwen 的一层。

## 2. 决策记录

| 决策 | 选择 | 备选与放弃原因 |
|---|---|---|
| demo 的完成标准 | 训练好的模型在行为级电路仿真器上仍给出正确输出 | 只在软件上跑:不覆盖硬件侧主张。整模型 SPICE:元件规模在 10^5 量级以上,ngspice 跑不动 |
| 训练数据 | 常用语料 TinyStories 的一个小切片,demo 句子取自切片本身 | 手写 5 组问答:模型内部表示没有语言统计;先预训练再微调问答:多一个阶段,demo 的判据不如"复现语料原句"干净 |
| 分词 | Qwen3.5-0.8B 的预训练分词器,固定 Hub 修订号;嵌入与输出头只覆盖切片里出现过的 token(紧凑索引) | 从切片自建词表:与后续任何 Qwen 相关工作不兼容。完整 15 万词表:输出头约 1900 万个元件,训练时间被输出头主导,而切片只用到约 3 千个 token |
| 电路里的神经元 | 理想 LIF 加阵列非理想性 | ADM 物理模型:Table 1 模型在 R_c ≥ 250 MΩ 下与正文定性不符,待 SI 核对(见 stage1-progress.md 2026-09-19 节)。神经元工厂接口保留,模型修正后可直接换入 |
| 训练路径 | 先训浮点 GDN 模型,再换成阈值激活并微调,再打开膜电位跨 token 携带并微调 | 直接训脉冲模型:一步到位,但不演练原 Stage 2.1 的转换路径,也得不到"每一步转换各损失多少"的数字 |
| 代码位置 | 新建顶层目录 `step2-toy-demo/`,通过可编辑安装引用 `step1-spiking-layer/snn_spec` | 放进 step1 目录:混淆 Stage 0/1 的参考实现与 demo 代码 |

## 3. 任务与数据

**语料切片。** TinyStories 训练集(Hugging Face `roneneldan/TinyStories`,许可 CDLA-Sharing-1.0)按流式读取开头的若干篇故事,直到句数达到约 4000 句(约 5 万个 token,预计 300 篇左右);实际篇数记录在切片文件里。故事按句号、问号、感叹号切成句子,保留原始大小写。每句是一条独立训练序列,序列开头加 begin、结尾加 end。begin、end、pad 是紧凑索引里的三个特殊 token,不对应任何 Qwen id,解码时忽略。不含特殊 token 超过 40 个 token 的句子丢弃。

**为什么按句子而不按故事。** 折叠 T=1 下 GDN 状态和膜电位都跨 token 携带。demo 要求模型从零状态、只给一句话的开头就补全这句话;训练序列也从零状态开始,训练条件与 demo 条件一致。每次电路运行不超过 40 个周期。

**分词器与紧凑索引。** 用 `tokenizers` 库加载 Qwen3.5-0.8B 的 `tokenizer.json`,记录 Hub 修订号与文件 SHA-256。切片里出现过的 Qwen token id 按出现顺序映射到紧凑索引 0..V−1,V 预计约 3000。模型的嵌入表和输出头按 V 建;分词与解码仍走 Qwen 分词器,所以 demo 输入输出的文本与 Qwen 一致。紧凑词表连同每个 token 的文本一起存入切片文件,解码不需要分词器。

**入库文件。** `step2-toy-demo/data/tinystories-slice/` 存放:原始句子、每句的 Qwen token id、紧凑映射表、来源与许可说明、故事编号范围、构建脚本参数、SHA-256。训练、转换、电路运行只读这个目录,不需要网络。分词器只在构建切片和 demo 命令输入新前缀时需要。

**demo 句子。** 从切片里按固定种子选 5 句,条件:不含特殊 token 的长度为 12–16 个 token;前 5 个 token 组成的前缀在整个切片里只出现一次。前 5 个 token 作为输入前缀,其余 token 是补全目标。TinyStories 的开头句("Once upon a time, there was")在大量故事里重复,前缀唯一性条件是为了让补全目标有唯一答案。

**训练信号。** 对每条序列每个位置做下一 token 交叉熵,teacher forcing,pad 位置不计入损失。目标是把切片背下来,不设留出集。

## 4. 模型

四个阶段共用一套骨架和权重形状,只有激活函数和膜电位设置不同。

**尺寸(全部放在一个配置对象里)。** 隐层 d=128;2 个块;key 头和 value 头各 4 个,dk=dv=32;FFN 中间维 256;紧凑词表约 3000。约 110 万个权重,其中嵌入和输出头占三分之二。电路元件折算:忆阻器约 70 万、gain cell 8192、神经元 2560、比较器 1280(每个三值量化器每通道两个比较器)。

**逐 token 数据流(两种模型相同)。** 嵌入查表得到该 token 的累加器初值 h(电路上的唯一 DAC)→ 块 1 → 块 2 → 输出头 → argmax。每个块按规范:块输入量化 → GDN(形式 A,`SpikingGDN.core`)→ 累加 → 块输入量化 → SwiGLU → 累加。门用 `SigmoidGates`,α 钳在 [0.5, 0.99],门的输入是块输入。不用 FIR,不用归一化层。

**A0 浮点模型。** 块输入就是 h 本身;q、k 做 L2 归一化(与 Gated DeltaNet 原式一致),v 与读出 o 连续;SwiGLU 用 SiLU;输出头直接读 h。它是普通的小型 GDN 语言模型,给出后续所有阶段的困惑度基线。

**A1 可脉冲化模型。** 装入 A0 的权重,把每个激活点换成规范 S0.5 编码表规定的阈值阶跃:块输入三值、q 二值、k 三值乘 1/√dk、v 三值、W_O 前后各一个三值、SwiGLU 门路二值、内容路三值、FFN 输出三值、输出头前的量化三值。q、k 的 L2 归一化去掉,换成 k 的固定缩放。阈值可学,初值由 A0 在切片上的激活统计量定:二值点取输入的 75 分位数,三值点取输入绝对值的 75 分位数,使每个神经元约在四分之一的 token 上发放。A0 里门的输入是 h,A1 起是三值 x;门的权重照搬,靠 A1 的微调适应。膜电位泄漏系数 leak=0,神经元逐 token 无状态。这相当于原 Stage 2.1 的第 3、5、6、7 类改造一次做完。

**B 折叠转换。** 同一网络,leak 改为规范默认值 0.9,权重和阈值不变。膜电位开始跨 token 携带。这是原 Stage 2.1 的"折叠转换"。

**C 微调后的脉冲模型。** B 经短微调后的结果,是电路运行的输入。

**对 `snn_spec` 的改动。** `SpikingBlock`、`SpikingGDN`、`SpikingSwiGLU` 的构造函数增加 `learn_thr` 参数并传给神经元,默认 False,现有行为不变。`step1-spiking-layer/` 增加最小的 `pyproject.toml`,使 `snn_spec` 可以 `pip install -e` 安装。其余代码原样使用。

## 5. 训练、转换与微调

| 阶段 | 起点 | 训练 | 停止条件 |
|---|---|---|---|
| A0 浮点 | 随机初始化 | Adam,batch 64 句,梯度裁剪 1.0,学习率 3e-3 余弦衰减,上限 6000 步 | 5 句补全全部正确,且切片 token 准确率在最近 500 步内的提升不到 0.1 个百分点 |
| A1 可脉冲化 | A0 权重 + 阈值初值 | 同上,leak=0,上限 2000 步 | 同上 |

切片里存在相同前缀接不同续写的句子,token 准确率不可能到 100%,所以停止条件用平台判断而不是固定阈值。
| B 零训练转换 | A1 权重,leak=0.9 | 不训练,只评估 | — |
| C 微调 | B | 同上,leak=0.9,梯度经代理梯度穿过膜电位,学习率 1e-3,上限 1500 步 | 5 句补全全部正确,且切片准确率回到 A1 的 1 个百分点以内 |

种子固定为 0;某阶段在步数上限内达不到停止条件时依次换种子 1、2,三个种子都失败才调整超参数并记录。设备自动选择:本机 CPU 约一到两小时完成 A0,GPU 上为分钟级。单线程运行,记录 torch 版本与设备,与现有实验一致。

**统一评估函数。** 输入任意阶段的模型(含第 6 节的电路模型),输出:切片上 teacher forcing 的 token 准确率与困惑度;5 个 demo 前缀的贪心补全(最多生成 40 个 token,遇 end 停止)及每句是否与目标完全一致。所有阶段和电路运行都用这一个函数,最终表格的每一列同一口径。

**产物。** A0、A1、C 三个检查点;`results/toy-demo.json` 记录全部数字;`results/toy-demo.md` 是结果页。

## 6. 电路运行与 demo

**接线。** 两个训练好的块各自包一层现有的 `BehavioralBlock`,神经元用默认的 `IdealLIFModel`。嵌入查表充当 DAC。输出头由三部分组成:比较器读末端累加器得三值脉冲、`CrossbarModel` 承载输出头权重、对 bitline 电流取 argmax(数字式赢者通吃)。token 顺序与软件模型相同:一个 token 依次过两个块,再下一个 token。

**两种配置。**

| 配置 | crossbar | gain cell | 比较器 | 要求 |
|---|---|---|---|---|
| 理想 | 无量化、无误差、无噪声 | 无误差,保持因子 1 | 无失调 | 与软件模型逐脉冲相同(现有单块测试已证明这一性质,本计划扩展到两块加输出头) |
| 标称 | 16 级电导,编程误差 2%,读噪声 1% | 写入误差 2%,读误差 2%,保持因子 0.999 | 失调为阈值的 2%,无迟滞 | 第 7 节的通过判据 |

标称值是假设,不是测量值:Stage 1 表征的是时序和 α,没有标定这几项。它们写在配置对象里,Stage 1 补测后替换。另做编程误差 0、2%、5%、10% 的扫描,给出裕度。

**两种模式。**

- **teacher forcing 对照**(原 S2.4 的方法):软件模型 C 在 5 句 demo 加固定 200 句切片样本上带记录运行;电路吃同样的 token 序列,门值用软件记录的 α、β(`ext_gates`)。逐 token 计算 `snn_spec.metrics` 里的指标:两个块的注意力输出与 FFN 输出脉冲按正负分的 precision/recall、符号错误率、状态误差(除以命题上界)、输出头 argmax 一致率。
- **自由运行**:电路从前缀开始自己生成,门值由电路自己的三值块输入经理想门函数算出(`BehavioralBlock` 在不给 `ext_gates` 时的现有行为)。门生成电路按规范 S0.6 不在本计划范围内。

**demo 命令。** 一条命令对 5 个前缀各打印目标句和五个阶段(A0、A1、B、C、电路标称)的补全,每项标明是否完全一致;随后打印指标表。可选的 `--prompt` 参数用 Qwen 分词器处理新前缀,遇到紧凑词表之外的 token 时报错并列出该 token,否则并排打印软件与电路的补全。

## 7. 验收标准

| 项 | 判据 |
|---|---|
| A0 | 5 句补全全部正确 |
| A1 | 5 句补全全部正确;相对 A0 的困惑度变化记录在案 |
| B | 只记录,不要求通过 |
| C | 5 句补全全部正确;切片准确率在 A1 的 1 个百分点以内 |
| 电路·理想 | 与 C 逐脉冲相同、逐 token 的 argmax 相同 |
| 电路·标称·自由运行 | 5 句补全全部正确 |
| 电路·标称·teacher forcing | 每个块两个输出的正负 precision/recall 均 ≥ 0.9;符号错误率 ≤ 2%;状态误差最大值 ≤ 0.1;输出头 argmax 与软件模型一致的 token 比例 ≥ 99% |

电路·标称不过时按原 S2.7 的顺序隔离:先只比对 FFN 子块,再只比对 GDN 子块,再只比对状态阵列读写。

## 8. 代码布局、测试与文档

**目录。**

```
step2-toy-demo/
  README.md                 运行方式
  requirements.txt          torch;-e ../step1-spiking-layer
  requirements-data.txt     tokenizers、datasets,仅构建切片和 --prompt 需要
  pytest.ini  conftest.py
  toy_demo/
    config.py     尺寸、超参数、非理想性数值、种子
    data.py       读切片、紧凑词表、批处理、demo 句子选择
    model.py      A0 浮点模型与 A1/B/C 脉冲模型
    convert.py    阈值初始化、leak 切换
    train.py      训练循环、统一评估函数
    circuit.py    多块电路模型、输出头、两种模式、指标
    run.py        A0 → A1 → B → C → 电路,写结果
    demo.py       demo 命令
    build_slice.py  一次性构建切片,需要网络
  data/tinystories-slice/
  results/
  tests/
```

**测试(全部离线、秒级)。**

- 切片能加载且 SHA-256 一致;紧凑索引与 Qwen id 往返一致;5 个 demo 前缀在切片里唯一;没有句子超过 40 个 token。
- A0 与 A1 的权重形状一致;A0 权重装入 A1 没有缺失键。
- 阈值初始化在合成输入上给出目标发放率。
- leak=0 时神经元逐 token 无状态:同一输入在不同历史下输出相同。
- 理想原语的电路模型在随机权重、短序列上与脉冲模型逐脉冲相同,包括输出头。
- teacher forcing 指标与自由运行解码在微型模型上能端到端跑通。
- 20 步训练冒烟测试:合成数据上损失下降。

完整流水线耗时数小时,作为实验脚本运行并记录结果,不进测试。

**文档。** 本文件是设计文档。project-plan.md 的标题处和 Stage 2–4 各节开头加一句"已被 toy-demo-plan.md 取代",正文不改。stage1-progress.md 的"下一步"里关于下载 Qwen 权重与 LoRA 的一条改为指向本计划。step1-spiking-layer/README.md 增加指向 step2 的一句。step2-toy-demo/README.md 写运行命令。结果页由流水线生成。

## 9. 假设与未决项

- 非理想性的标称值是假设值,来源见第 6 节。
- ADM 物理神经元不进本计划的电路环路;神经元工厂接口保留。
- 门生成电路不在范围内,电路里的门由理想函数从电路自身的脉冲算出。
- 不以 SPICE 级仿真为目标;元件规模约 70 万,超出 ngspice 的承受范围。
- 累加器 h 的取值范围随深度增长,2 个块下上界为嵌入幅度加 4;块输入量化器阈值可学,预计足够,训练时记录 h 的实际范围。
- CPU 训练时间是估计值,以实际运行为准。
