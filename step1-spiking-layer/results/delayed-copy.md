# 延迟复制探针：2026-09-13

对应 project-plan.md S0.10。d=16、单头、一个完整 SpikingBlock，FFN 中间维 32；SigmoidGates、膜阈值 0.3、膜泄漏 0.9、入口阈值初始 1。另加连续线性分类头，仅用于任务读出，不属于电路层。

任务：首 token 给出四种符号之一，随后三个全零 token，最后固定查询 token。标签只在首 token 出现，末尾输入对所有类别相同，每段序列重新初始化状态。均衡枚举四类，每类重复八次；Adam lr=0.003，300 步，梯度裁剪 1，CPU 单线程。

| 种子 | 正常 | 每步清零全部状态 | 每步清零 S | 每步清零膜电位 |
|---|---|---|---|---|
| 0 | 100% | 25% | 100% | 75% |
| 1 | 100% | 25% | 100% | 25% |
| 2 | 100% | 25% | 50% | 25% |

最终评估枚举全部四类，是小任务拟合检查，不是独立数据集泛化评估；正确率粒度为 25%。消融使用同一训练后模型，未重新训练。清零 S 仅消除它的跨 token 存储，当前步仍可写入读出。

未训练的空白长度 1/5/7：seed 0、1 均 100%；seed 2 为 100%/75%/50%。

结论：此配置可用代理梯度学会简单延迟复制，并依赖跨 token 状态。两个种子移除 S 记忆后仍完全正确，说明膜电位通路足以完成此任务；不能据此宣称 GDN 关联检索能力已验证。区分两条记忆通路的后续探针见 `associative-recall.md`。

未使用真实 Qwen 权重、真实文本或 SPICE。本实验不改变规范冻结状态。原始训练记录和 PyTorch 版本见 delayed-copy.json。
